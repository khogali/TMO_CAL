import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tmobile.salescenter',
  appName: 'T-Mobile Sales Center',
  webDir: 'dist'
};

export default config;
